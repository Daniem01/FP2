#include <iostream>
using namespace std;

const int MAX_TAM = 20;
typedef int tMatriz[MAX_TAM][MAX_TAM];

int main(){
    tMatriz matriz;
    int N, M;

    while(){
        for(int i = 0; i < N; i++){
            for(int j = 0; j < M; j++){
                cin >> matriz[i][j];
            }
        }
    }

    return 0;
}